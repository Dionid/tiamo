
type Query {
    dummy: String
}

type MutationResponse {
    success: Boolean!
    message: String
}

input RegisterUserInput {
    email: String!
}

input ApproveEmailByTokenInput {
    token: String!
    email: String!
}

input LoginPasswordlessByEmailInput {
    email: String!
}

type Mutation {
    registerUser(req: RegisterUserInput): MutationResponse
    approveEmailByToken(req: ApproveEmailByTokenInput): MutationResponse
    loginPasswordlessByEmail(req: LoginPasswordlessByEmailInput): MutationResponse
}
